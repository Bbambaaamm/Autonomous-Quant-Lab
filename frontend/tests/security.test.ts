import {describe,it,expect} from "vitest";import fs from "node:fs";import path from "node:path";
describe("paper-only hranice",()=>{it("navigace neobsahuje live akci ani generic proxy",()=>{const layout=fs.readFileSync(path.join(process.cwd(),"app/layout.tsx"),"utf8");expect(layout).not.toMatch(/enable.live|\/proxy\?url/i);expect(layout).toContain("LIVE ABSENT")});it("akce vyžadují serverovou odpověď a explicitní potvrzení",()=>{const actions=fs.readFileSync(path.join(process.cwd(),"app/actions.ts"),"utf8");expect(actions).toContain("confirmation");expect(actions).toContain("if(!r.ok)")})});

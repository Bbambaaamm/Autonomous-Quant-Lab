import Link from "next/link";import {api} from "@/lib/api";import {Card,JsonTable,Status,na} from "@/components/ui";export const dynamic="force-dynamic";
export default async function Home(){const [o,data,research,deployments,runtime]=await Promise.all([api<Record<string,any>>("/operator/overview"),api<Record<string,any>>("/operator/data-health?membership_limit=25&membership_offset=0"),api<Record<string,any>>("/operator/research/experiments?limit=5&offset=0"),api<Record<string,any>[]>("/operator/deployments/summary?limit=50"),api<Record<string,any>>("/operator/automation")]);const recentDeployments=deployments.slice(0,5);return <><h1>Control Center <span className="badge">PAPER ONLY</span></h1><p className="muted">Jednotný read model pro data → research → eligibility → deployment → monitoring → autonomous paper runtime. Všechny rozhodující kontroly zůstávají server-side.</p><div className="grid"><Card title="Trading state" value={<Status value={o.trading_state}/>} asOf={o.as_of}/><Card title="Reconciliation" value={<Status value={o.reconciliation_safe===true?"SAFE":"UNSAFE"}/>}/><Card title="Market data" value={<Status value={data.fresh?"READY":"MISSING_SESSION_DATA"}/>} asOf={data.latest_successful_session}/><Card title="Corporate actions" value={<Status value={data.provider?.supports_actions===true?"SUPPORTED":data.provider?.supports_actions===false?"UNSUPPORTED":"CAPABILITY_NOT_REPORTED"}/>}/><Card title="Monitoring" value={<Status value={o.monitoring_state??"NOT_ENROLLED"}/>} asOf={o.monitoring_id}/><Card title="Autonomous runtime" value={<Status value={o.autonomous_readiness}/>} asOf={o.next_scheduled_paper_cycle}/><Card title="Workers" value={`${o.healthy_worker_count} healthy · ${o.stale_worker_count} stale`}/><Card title="Dead letters" value={<Status value={o.dead_letter_count===0?"0":`FAILED ${o.dead_letter_count}`}/>}/></div><div className="grid"><section><h2>Data readiness</h2><div className="card"><p>Latest completed XNYS: <strong>{na(data.latest_completed_session)}</strong></p><p>Latest successful data: {na(data.latest_successful_session)}</p><p>{data.current_observation_count}/{data.relevant_instrument_count} relevant instruments observed.</p><Link href="/data">Open Data control-plane →</Link></div></section><section><h2>Runtime</h2><div className="card"><p>Automation: <Status value={runtime.enabled?"ENABLED":"DISABLED"}/></p><p>{runtime.jobs?.filter((x:any)=>x.enabled).length??0} enabled jobs · {runtime.runs?.length??0} recent runs</p><Link href="/operations">Open Operations →</Link></div></section></div><h2>Latest research experiments</h2><JsonTable rows={research.items}/><h2>Recent paper deployments</h2><JsonTable rows={recentDeployments}/>{recentDeployments[0]&&<div className="card"><strong>Latest runtime identity</strong><p><code>{na(recentDeployments[0].runtime_manifest_hash)}</code> · manifest v{na(recentDeployments[0].runtime_manifest_version)}</p></div>}</>}

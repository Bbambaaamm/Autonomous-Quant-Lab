import {autonomousScheduleAction} from "@/app/actions";import {Empty,JsonTable,Status} from "@/components/ui";import {api} from "@/lib/api";export const dynamic="force-dynamic";
export default async function Detail({params}:{params:Promise<{identity:string}>}){const {identity}=await params;const d=await api<Record<string,any>>(`/operator/strategies/${encodeURIComponent(identity)}`);return <><h1>{d.name} · {d.version}</h1><pre className="card">{JSON.stringify(Object.fromEntries(Object.entries(d).filter(([k])=>!["experiments","deployments"].includes(k))),null,2)}</pre><h2>Research evidence</h2><JsonTable rows={d.experiments}/><h2>Paper deployments</h2>{!d.deployments.length?<Empty/>:d.deployments.map((x:Record<string,any>)=><section className="card" key={x.deployment_id}><h3>{x.deployment_id}</h3><Status value={x.status}/><h4>Runtime configuration</h4><p>Manifest hash: <code>{String(x.runtime_manifest_hash??"LEGACY / MISSING")}</code> · version {String(x.runtime_manifest_version??"N/A")}</p><p>Autonomous scheduling: <strong>{x.autonomous_scheduling?"ON":"OFF"}</strong></p><p>Signal {String(x.next_signal_session)} → execution {String(x.next_execution_session)} · next refresh {String(x.next_expected_refresh??"N/A")}</p><pre>{JSON.stringify(x.last_job_run??{last_job_run:"N/A"},null,2)}</pre><form action={autonomousScheduleAction}><input type="hidden" name="deployment_id" value={x.deployment_id}/><input type="hidden" name="action" value={x.autonomous_scheduling?"disable":"enable"}/><label>Auditní důvod <input name="reason" required minLength={3} maxLength={1000}/></label> <button>{x.autonomous_scheduling?"Disable":"Enable"}</button></form></section>)}</>}

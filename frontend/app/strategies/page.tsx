import Link from "next/link";
import { api } from "@/lib/api";
import { Empty } from "@/components/ui";
export const dynamic = "force-dynamic";
export default async function Strategies() { const rows = await api<Record<string, unknown>[]>("/operator/strategies"); return <><h1>Strategie</h1><p className="muted">Délka cenové historie (lookback) a frekvence rebalancování patří ke konkrétnímu nasazení. Jedna strategie může mít více nastavení.</p>{!rows.length ? <Empty>Žádné registrované strategie.</Empty> : <div className="card table-scroll"><table><thead><tr><th>Identita</th><th>Název</th><th>Verze</th><th>Parametry nasazení</th></tr></thead><tbody>{rows.map(r => <tr key={String(r.strategy_identity)}><td><Link href={`/strategies/${r.strategy_identity}`} title={String(r.strategy_identity)}>{String(r.strategy_identity).slice(0, 12)}…</Link></td><td>{String(r.strategy_name)}</td><td>{String(r.strategy_version)}</td><td><Link href={`/strategies/${r.strategy_identity}`}>Zobrazit nastavení nasazení →</Link></td></tr>)}</tbody></table></div>}</>; }

import {api} from "@/lib/api";import {JsonTable,Status,Empty} from "@/components/ui";import {monitoringAction} from "@/app/actions";
export const dynamic="force-dynamic";
export default async function Monitoring({params}:{params:Promise<{id:string}>}){const {id}=await params;const d=await api<Record<string,any>>(`/operator/monitoring/${encodeURIComponent(id)}/comparison`);const state=String(d.monitoring.state);return <><h1>Monitoring {id}</h1><div className="card"><Status value={state}/><p>{d.monitoring.state_reason}</p><small>Started {d.monitoring.started_at} · ended {d.monitoring.ended_at??"N/A"}</small></div><h2>Policy</h2>{d.policy?<pre className="card">{JSON.stringify(d.policy.config,null,2)}</pre>:<Empty/>}<h2>Oddělená OOS baseline</h2>{d.baseline?<pre className="card">{JSON.stringify({metrics:d.baseline.oos_metrics,returns:d.baseline.oos_returns,equity:d.baseline.oos_equity},null,2)}</pre>:<Empty/>}<h2>Realized paper</h2><JsonTable rows={d.paper_points}/><h2>Historická hodnocení</h2><JsonTable rows={d.evaluations}/>{state!=="RETIRED"&&<form action={monitoringAction} className="card"><h2>Explicitní operator akce</h2><input type="hidden" name="id" value={id}/><label>Akce <select name="action"><option value="pause">PAUSE</option><option value="resume">RESUME</option><option value="retire">RETIRE</option></select></label> <label>Potvrzení <input name="confirmation" required placeholder="PAUSE / RESUME / RETIRE"/></label> <label>Důvod <input name="reason" required minLength={3} maxLength={1000}/></label> <button className="danger">Provést až po odpovědi serveru</button></form>}</>}

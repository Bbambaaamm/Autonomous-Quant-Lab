import {api} from "@/lib/api";import {eligibilityAction,promotionAction} from "@/app/actions";import {JsonTable,Status} from "@/components/ui";
export const dynamic="force-dynamic";
type Decision={status:string;policy_id:string;policy_version:number;evaluated_at:string;reason:string;actor:Record<string,unknown>;rules:Record<string,unknown>[]};
export default async function ExperimentDetail({params}:{params:Promise<{id:string}>}){const {id}=await params;const experiment=await api<Record<string,unknown>&{eligibility:Decision|null}>(`/operator/research/experiments/${encodeURIComponent(id)}`);const decision=experiment.eligibility;return <><h1>Experiment {id}</h1><pre className="card">{JSON.stringify({...experiment,eligibility:undefined},null,2)}</pre><h2>Eligibility</h2>{decision?<div className="card"><Status value={decision.status}/><p>Policy {decision.policy_id} / v{decision.policy_version} · {decision.evaluated_at}</p><p>Důvod: {decision.reason}</p><p>Actor: {JSON.stringify(decision.actor)}</p><JsonTable rows={decision.rules}/></div>:<p className="card">NOT EVALUATED</p>}<form action={eligibilityAction} className="card"><input type="hidden" name="id" value={id}/><label>Důvod evaluace <input name="reason" required minLength={3} maxLength={1000}/></label> <button>Evaluate eligibility</button></form><form action={promotionAction} className="card"><input type="hidden" name="id" value={id}/><label>Důvod promotion <input name="reason" required minLength={3} maxLength={1000}/></label> <button disabled={decision?.status!=="ELIGIBLE"}>Promote to PAPER_CANDIDATE</button></form></>}

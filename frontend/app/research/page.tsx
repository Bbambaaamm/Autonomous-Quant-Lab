import Link from "next/link";import {api} from "@/lib/api";import {session} from "@/lib/auth";import {JsonTable} from "@/components/ui";import {MutationForm} from "@/components/mutation-form";import {experimentAction} from "@/app/actions";export const dynamic="force-dynamic";
export default async function Research(){const [d,user]=await Promise.all([api<{items:Record<string,unknown>[];total:number}>("/operator/research/experiments?limit=50&offset=0"),session()]);return <><h1>Research evidence</h1><p>{d.total} persistentních experimentů · OOS metriky a decision vrací autoritativní backend.</p><p>{d.items.map(x=><Link className="badge" key={String(x.id)} href={`/research/${String(x.id)}`}>Experiment {String(x.id)}</Link>)}</p><JsonTable rows={d.items}/>{user?.role==="ADMIN"?<MutationForm action={experimentAction} title="Run supported chronological experiment" submit="Run paper research experiment"><label>Snapshot ID<input name="snapshot_id" required/></label><label>Strategy name<input name="strategy_name" required/></label><label>Strategy version<input name="strategy_version" required/></label><label>Parameter configs (JSON array)<textarea name="parameter_configs" required defaultValue={'[{"lookback":20}]'}/></label><label>Code SHA (40 chars)<input name="code_sha" required minLength={40} maxLength={40}/></label><label>Deterministic seed<input name="seed" type="number" defaultValue="42"/></label><label>Audit reason<textarea name="reason" required minLength={3}/></label></MutationForm>:<p className="card muted">Read-only role: experiment mutation není dostupná.</p>}</>}
